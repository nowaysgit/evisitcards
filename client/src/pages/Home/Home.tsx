import React, {FC} from 'react';
import ProfileInfo from "../Profile/ProfileInfo";

const Home: FC = () => {

    return (
        <div>
            <ProfileInfo/>
        </div>
    );
};

export default Home;